<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
 <mapper namespace="com.nctclub.mapper.UserMapper">
 	<insert id="insert" parameterType="userDTO">
		INSERT INTO users(subject, contents, writer)
		VALUES(#{subject}, #{contents}, #{writer}) 		
	</insert>
	
	<select id="userLogin" parameterType="userDTO" resultType="userDTO">
  		SELECT user_id, password, nickname FROM users WHERE user_id = #{user_id}
  	</select>
 </mapper>